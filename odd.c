#include <stdio.h>

int main() {
	int low,high,i;
	printf("enter the interval");
	scanf("%d%d",&low,&high);
	    for(i=low;i<=high;i++)
	    
	       { if(i%2!=0)
	       
printf("%d",i);
}
	return 0;
}
